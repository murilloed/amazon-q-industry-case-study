# Scripts

Run the conservative release scanner before committing:

```bash
python scripts/verify_public_release.py .
```

The scanner is a guardrail, not proof of anonymization or legal permission.
Manual review and authorization remain required.
